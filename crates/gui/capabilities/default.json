{
  "$schema": "https://schema.tauri.app/capability",
  "identifier": "default",
  "description": "The desktop window reads and edits the local index.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
